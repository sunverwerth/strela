module External {
    import Std.IO.*;

    function main(args: String[]): int {
        println(sqrt(1024));
        println(sqrt(256));
        println(sqrt(64));
        println(sqrt(16));
        println(sqrt(4));
        return 0;
    }
}